name: shikkha_ai
description: ShikkhaAI - Bengali QA Voice Assistant for Kids
version: 1.0.0+1

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  speech_to_text: ^6.3.0
  flutter_tts: ^3.6.3
  tflite_flutter: ^0.10.0
  tflite_flutter_helper: ^0.3.0

flutter:
  uses-material-design: true
  assets:
    - assets/model.tflite
    - assets/tokenizer.json
